Ask user to pick Rock, Paper or Scissors (R / P / S)
Computer randomly picks R, P or S


            If users answer = computer's answer == DRAW
            If User picks S, computer picks R == LOSE
            If User picks S, computer picks P == WIN
            If User picks P, computer picks S == LOSE
            If User picks P, computer picks R == WIN
            If User picks R, computer picks P == LOSE
            If User picks R, computer picks S == WIN




